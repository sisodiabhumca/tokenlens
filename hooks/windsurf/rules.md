# TokenLens — Windsurf rules

Always run shell commands through `tokenlens` to compress output before it enters context.

✅ `tokenlens git diff`
✅ `tokenlens npm run build`
❌ `git diff`

Use `tokenlens gain` to verify savings.
